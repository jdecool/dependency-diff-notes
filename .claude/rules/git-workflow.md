# Git workflow

- **Commit messages**: short, imperative, **in English** (e.g. `Add Copilot detection`, `Fix trailer parsing`).
- **One branch per topic**: avoid mixing several distinct functional changes in the same branch/PR.
- Never commit or push without an explicit request from the user.
